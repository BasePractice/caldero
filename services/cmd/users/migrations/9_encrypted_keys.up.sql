-- Признак того, что приватный ключ хранится зашифрованным. Существующие
-- ключи остаются открытыми: перешифровать их миграцией нельзя — мастер-ключ
-- живёт в окружении сервиса, а не в базе.
ALTER TABLE keys
    ADD COLUMN IF NOT EXISTS encrypted BOOLEAN NOT NULL DEFAULT FALSE;
