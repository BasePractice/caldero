-- Текущий ключ подписи и набор для JWKS выбираются с сортировкой по времени.
CREATE INDEX IF NOT EXISTS keys_created_at_idx ON keys (created_at DESC);

-- Аутентификация ищет пользователя по имени; UNIQUE уже даёт индекс,
-- поэтому отдельный не нужен. Роли выбираются по пользователю, и первичный
-- ключ (user_id, role) покрывает этот запрос префиксом.
